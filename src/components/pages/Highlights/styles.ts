import { makeStyles } from "@material-ui/core";


const useStyles = makeStyles((theme) => ({
    root: {
      
    },
    recentContact: {
      margin: "0 5px", 
      userSelect: "none", 
      cursor: "pointer"
    }
  }));

  export default useStyles;
