import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    root: {

    },
    inputElement: {
        borderRadius: "20px",
        border: "0",
        outline: "none",
        background: "#e0e0e0",
        padding: "12px 15px",
        minWidth: "80vw",
        minHeight: "16px",
        

        resize: "none"
    }
}));

export default useStyles;